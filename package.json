{
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.sql": "prettier --write"
  },
  "devDependencies": {
    "husky": ">=8",
    "lint-staged": ">=13",
    "prettier": "^2.8.1",
    "prettier-plugin-sql": "^0.12.1"
  }
}
